docs {
  # Using the Startrek-API with Bruno
  
  Example of using bruno to test and document yaour API.
  
  1. `Get ships` shows a simple get get request with one passing and one failing test.
  2. `Create ship` Asserts the http status code is correct. Tests for the status code. Another test verifies each field in the returned data is as expected
}
